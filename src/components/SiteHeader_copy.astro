---
import { Image } from "astro:assets";
// for light mode
import logoLight from "../assets/logo-light.webp";
// import logoLightBackground from "../assets/logo-light-background.webp";
// for dark mode
import logoDark from "../assets/logo-dark.webp";
// import logoDarkBackground from "../assets/logo-dark-background.webp";
---

<header class="site-header">
    <div class="container">
        <!-- <a href="/" class="site-logo">MiSitio</a> -->
        <!-- <a
                    href="/"
                    class="logo-link"
                    aria-label="Carolina Prime Construction & Remodeling LLC - Ir al inicio"
                > -->
        <!-- <img
                    src="/favicon.svg"
                    alt="Carolina Prime Construction & Remodeling LLC Logo"
                    width="100"
                    height="100"
                    loading="eager"
                    decoding="async"
                    aria-hidden="false"
                /> -->

        <!-- Imagen para Modo Claro -->
        <!-- <Image
                        src={logoLight}
                        alt="Carolina Prime Construction & Remodeling LLC Logo"
                        width={160}
                        height={160}
                        loading="eager"
                        decoding="async"
                        class="logo-theme-light"
                    /> -->
        <!-- Imagen para Modo Oscuro -->
        <!-- <Image
                        src={logoDark}
                        alt="Carolina Prime Construction & Remodeling LLC Logo"
                        width={160}
                        height={160}
                        loading="eager"
                        decoding="async"
                        class="logo-theme-dark"
                    /> -->
        <!-- </a> -->

        <a
            href={`${import.meta.env.BASE_URL}`}
            class="logo-link"
            aria-label="Carolina Prime Construction & Remodeling LLC - Ir al inicio"
        >
            <Image
                src={logoLight}
                alt="Carolina Prime Construction & Remodeling LLC"
                width={120}
                height={100}
                class="logo-theme-light"
                loading="eager"
                decoding="async"
            />

            <Image
                src={logoDark}
                alt="Carolina Prime Construction & Remodeling LLC Logo"
                width={120}
                height={100}
                loading="eager"
                decoding="async"
                class="logo-theme-dark"
            />
        </a>

        <button
            class="nav-toggle"
            id="nav-toggle"
            aria-expanded="false"
            aria-controls="site-nav"
            aria-label="Abrir menú de navegación"
        >
            <span class="hamburger-line"></span>
            <span class="hamburger-line"></span>
            <span class="hamburger-line"></span>
        </button>

        <nav
            class="site-nav"
            id="site-nav"
            aria-label="Menú de navegación principal"
        >
            <a href={`${import.meta.env.BASE_URL}`}>Home</a>
            <a href={`${import.meta.env.BASE_URL}index2`}>Home2</a>
            <a href={`${import.meta.env.BASE_URL}services`}>Services</a>
            <a href={`${import.meta.env.BASE_URL}gallery`}>Gallery</a>
            <!-- <a href={`${import.meta.env.BASE_URL}kci-steps`}>KCI Steps</a> -->
            <!-- <a href={`${import.meta.env.BASE_URL}buildings-trailers`}>Buildings & Trailers</a> -->
            <a href={`${import.meta.env.BASE_URL}about`}>About</a>
            <a href={`${import.meta.env.BASE_URL}contact`}>Contact</a>
            <!-- <ul>
                        <li><a href={`${import.meta.env.BASE_URL}`}>Home</a></li>
                        <li><a href={`${import.meta.env.BASE_URL}services`}>Services</a></li>
                        <li><a href={`${import.meta.env.BASE_URL}gallery`}>Gallery</a></li>
                        <li><a href={`${import.meta.env.BASE_URL}kci-steps`}>KCI Steps</a></li>
                        <li>
                            <a href={`${import.meta.env.BASE_URL}buildings-trailers`}
                                >Buildings & Trailers</a
                            >
                        </li>
                        <li><a href={`${import.meta.env.BASE_URL}about`}>About</a></li>
                        <li><a href={`${import.meta.env.BASE_URL}contact`}>Contact</a></li>
                    </ul> -->
        </nav>
    </div>
</header>

<style>
    /* body {
        display: flex;
        flex-direction: column; */
    /* Ocupa el 100% del alto de la ventana */
    /* min-height: 100vh; */
    /* Elimina márgenes por defecto */
    /* margin: 0;
    } */

    header {
        /* background-color: #f9f6fc;
        padding: 0px 20px;
        display: flex;
        align-items: center;
        justify-content: space-between; */

        /* Estilos para el contenedor del logo y texto */
        .logo-link {
            /* display: flex;
            align-items: center;
            gap: 12px;
            text-decoration: none;
            border-bottom: none; */
        }

        /* h1 {
            margin: 0;
        }

        ul {
            display: flex;
            list-style: none;
            gap: 16px;
        }

        a {
            text-decoration: none;
            border-bottom: 1px dashed;
        } */
    }

    /* main { */
    /* Hace que el main ocupe el espacio restante */
    /* flex: 1;
    } */

    /* Por defecto, mostramos la versión clara y ocultamos la oscura */
    /* .logo-theme-dark {
        display: none !important;
    }
    .logo-theme-light {
        display: block !important;
    } */

    /* Cuando el sistema del visitante está en modo oscuro (o si cambias el header a negro) */
    /* @media (prefers-color-scheme: dark) {
        .logo-theme-light {
            display: none !important;
        }
        .logo-theme-dark {
            display: block !important;
        }

        header {
            background-color: #2e2e2e !important;
        }

        header nav a {
            color: #fff !important;
        }
    } */

    .nav-toggle {
        display: flex;
        flex-direction: column;
        gap: 5px;
    }

    .logo-link {
        display: flex;
        align-items: center;
    }
    .logo-link img {
        /* height: 44px; */
        height: 50px;
        /* width: auto; */
        width: 70px;
        min-width: 70px !important;
    }
    /* Variantes de tema (solo si tienes 2 logos) */
    .logo-theme-dark {
        display: none;
    }
    @media (prefers-color-scheme: dark) {
        .logo-theme-light {
            display: none;
        }
        .logo-theme-dark {
            display: block;
        }

        /* Opcional: Ajusta el color del menú para que contraste en fondo oscuro */
        header {
            /* Fondo gris oscuro/negro */
            background-color: #2e2e2e !important;
        }

        header nav a {
            /* Texto de enlaces en blanco */
            color: #fff !important;
        }
    }
    /* @media (min-width: 525px) {
        .logo-link img {
            min-width: 90px !important;
            height: 70px;
        }
    } */

    /* footer {
        background-color: #f9f6fc;
        padding: 10px 20px;
        text-align: center;
        vertical-align: middle;
        width: 100%;
        position: relative;
    } */
</style>

<script>
    const toggle = document.getElementById("nav-toggle");
    const nav = document.getElementById("site-nav");
    toggle?.addEventListener("click", () => {
        const isOpen = nav?.classList.toggle("is-open");
        toggle.setAttribute("aria-expanded", String(isOpen));
    });
</script>
